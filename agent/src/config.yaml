seed: 42
dataset:
  name: "sst2"           # small sentiment dataset (we'll sample)
  n_per_class: 200
classical:
  model_name: "sentence-transformers/all-MiniLM-L6-v2"
quantum:
  n_qubits: 4
  dev: "default.qubit"
trainer:
  model: "svm"
  svm:
    C: 1.0
output_dir: "outputs/run1"
